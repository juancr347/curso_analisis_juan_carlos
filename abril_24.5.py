# Paso 1, crear una clase

Class Ordenador: 
    
    def__init__(self, fabricante, modelo, ano, precio, estado):
       self.fabricante = fabricante
       self.modelo = Modelo
       self.año = ano
       self.precio = precio
       self.estado = false
       
    def encender():
        self.estado = True
        
        def aplicar_descuento(self, descuento):
            if (descuento > 0 and descuento < 0,9):
            self.precio = self.precio - self.precio * descuento
    
 # Creacion de objetos, instancias
   
       ordenador1 = Ordenador('Asus', 'ASSA', 2008, 495)
       ordenador1-precio *= 0.9
       ordenador2 = Ordenador('msi', 'modern', 2021, 995)
       ordenador2.encender()
    